    int assignMiceHoles(int n , int arr[] , int brr[]) {
     sort(arr,arr+n);
     sort(brr,brr+n);
     int ans=0;
     for(int i=0;i<n;i++){
         ans=max(ans,abs(arr[i]-brr[i]));
     }
     return ans;
    }
