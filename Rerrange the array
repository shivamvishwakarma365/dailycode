void Solution::arrange(vector<int> &arr) {
    int n=arr.size();
for(int i=0;i<n;i++){
             int x=arr[i];
             int y=arr[x];
             arr[i]=x+(y%n)*n;
         }
         for(int i=0;i<n;i++){
             arr[i]/=n;
         }
}
