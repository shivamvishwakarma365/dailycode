bool check(vector<ll> a, vector<ll> b, int N) {
        sort(a.begin(),a.end());
        sort(b.begin(),b.end());
        for(int i=0;i<N;i++){
            if(a[i]!=b[i])return false;
        }
        return true;
    }
